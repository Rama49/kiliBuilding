// right_component.dart
import 'package:flutter/material.dart';

class RightComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      height: 100.0,
      // Contenu du composant de droite
    );
  }
}
